{
  "name": "milan-token",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "migrate": "truffle migrate --network testnet",
    "compile": "truffle compile --network ropsten",
    "verify": "truffle run verify MilanToken --network testnet",
    "test": "npx builder test",
    "coverage": "npx builder coverage",
    "flatten": "truffle-flattener contracts/MilanToken.sol > build/MilanToken.sol"
  },
  "dependencies": {
    "@openzeppelin/contracts": "4.1.0",
    "dotenv": "^9.0.2",
    "ethers": "^5.4.5",
    "truffle": "^5.3.11",
    "truffle-flattener": "^1.5.0",
    "truffle-hdwallet-provider": "^1.0.17",
    "truffle-plugin-verify": "^0.5.8",
    "web3": "^1.3.6"
  },
  "devDependencies": {
    "@openzeppelin/truffle-upgrades": "^1.7.0"
  }
}
